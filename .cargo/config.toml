# Do not set `runner = 'sudo -E'` here — it applies to every Cargo binary in the
# workspace (including the Tauri client) and breaks wallet connect in dev.
# Run boringtun with sudo explicitly; see README.md.
